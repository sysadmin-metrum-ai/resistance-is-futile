{
    auto_https off
}

:3003 {
    # API requests keep the /api prefix because FastAPI mounts routes there.
    handle /api/* {
        reverse_proxy api:8000
    }

    handle /drone/* {
        reverse_proxy api:8000
    }

    # Dashboard - everything else
    handle /* {
        reverse_proxy dashboard:3000
    }
}
